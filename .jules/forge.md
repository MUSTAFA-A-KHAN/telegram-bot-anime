YYYY-MM-DD - [Personal Stats Fix for Anime, Geography, WordGrid]

Learning: [When extending the `/mystats` personal stats dashboard for a new game, implement a `Get[Game]UserStatsByID` function in `service/leaderBoardList.go` to query the relevant MongoDB collection, and add the corresponding inline button and callback handler (e.g., `stats_[game]`) to both `controller/bot.go` and `controller/categoryBot/categoryBot.go`.]

Action: [I will apply it in the future by checking if a new game needs a personal stats view when I add a global leaderboard.]
